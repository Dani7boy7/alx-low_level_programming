#include "main.h"
#include <ctype.h>

/**
 *	* _isalpha - _ checks for alphabet
 *	* @c: char type
 *	* Return: 0 when succesful
 */

int _isalpha(int c)
{
	int i = _isalpha(c);

	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
