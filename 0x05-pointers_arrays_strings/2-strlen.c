#include "main.h"

/**
 *	* _strlen - gives the length of string
 *	* @s: the string
 *	* Return: the length
 *
 */

int _strlen(char *s)
{
	int logi = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}

	return (longi);
}
