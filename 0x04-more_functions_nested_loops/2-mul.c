#include "main.h"

/**
 *	* mul - multiplies two numbers
 *	* @a: the first number to be multiplied
 *	* @b: the second variable
 *	* Return: 0 when sucdesful
 *
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
