#include "main.h"

/**
 *	* _isdigit - checks for single digit
 *	* @c: the number to be checked
 *	* Return: 1 if c is the correct digit 0 if not
 *
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
